### НастроитьФормуРасчетаПриСозданииНаСервере

```bsl
// НастроитьФормуРасчетаПриСозданииНаСервере выполняет настройку параметров расчета для указанной формы
// при ее создании на сервере. Параметры расчета и их значения берутся из настройки пользователя.
// Имя реквизита формы должно совпадать с ключом параметров расчета целевых товарных запасов
// (см. ЦелевыеТоварныеЗапасыКлиентСервер.ПараметрыРасчета).
//
// Параметры:
//  Форма - УправляемаяФорма - форма, для которой выполняется настройка реквизитов с параметрами расчета;
//
Процедура НастроитьФормуРасчетаПриСозданииНаСервере(Форма) Экспорт
```
